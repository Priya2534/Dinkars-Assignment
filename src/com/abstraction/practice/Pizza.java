package com.abstraction.practice;
//you can not make data member as abstract e.g.abstract int size;(not allowed)

//..you can only apply abstract to methods

public abstract class Pizza {
	abstract void pizzabase(String flaour, int size);
}
